import React from 'react'
import "./skillset.css"

export default function Skillset(props) {
    const skillset_name=props.skillGroup;
    const skills=props.skills;
  return (
    <div class="container-fluid text-light">
           
    </div>
  )
}
